

//the app is the function only so we can run it by calling it directly  lets try NOT RECOMMENDED

//using variables in jsx
function App() {
  const username = "Kartikesh"
  return (
   <h1>Hello {username} From React with Vite!</h1>
        //this is not like template literals we can't write whole js here , this is only for evaluated expressions 
  )
}

export default App
